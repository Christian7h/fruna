---
//src/pages/index.astro

import Layout from '../layouts/Layout.astro';
import Favoritos from '../components/Favoritos.astro';
import Ground from '../components/icon/Ground.astro';
import Slider from '../components/Slider.astro';
import SliderTwo from '../components/SliderTwo.astro';
import ImageThird from '../components/ImageThird.astro';
---

<Layout title="Fruna - De la tierra a la Luna, no hay como tú y Fruna">
    <Slider/>
    <SliderTwo/>
    <ImageThird/>
    <Favoritos/>
</Layout>

<style>
  @keyframes bounceIn {
    0%, 20%, 40%, 60%, 80%, 100% {
      animation-timing-function: cubic-bezier(0.215, 0.610, 0.355, 1.000);
    }
    0% {
      opacity: 0;
      transform: scale3d(0.3, 0.3, 0.3);
    }
    20% {
      transform: scale3d(1.1, 1.1, 1.1);
    }
    40% {
      transform: scale3d(0.9, 0.9, 0.9);
    }
    60% {
      opacity: 1;
      transform: scale3d(1.03, 1.03, 1.03);
    }
    80% {
      transform: scale3d(0.97, 0.97, 0.97);
    }
    100% {
      opacity: 1;
      transform: scale3d(1, 1, 1);
    }
  }

  .bounce-in {
    animation: bounceIn 1s ease forwards;
  }
</style>